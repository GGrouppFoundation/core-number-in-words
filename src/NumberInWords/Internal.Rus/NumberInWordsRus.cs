#nullable enable

namespace GGroupp
{
    internal static partial class NumberInWordsRus
    {
        private static readonly RusWord EmptyRusWord = new(default, default, default, RusWordGender.Masculine);
    }
}